.row
  .col-sm-12
    %h3
      Your Order
      = link_to( "##{@order.id}", user_order_url(@order.user, @order) )
      has been confirmed."
.row
  .col-sm-12
    %ul
      - @order.line_items.each do |line_item|
        %li
          .row
            .col-sm-4
              = line_item.product.name
            .col-sm-2
              = "$#{line_item.product.price}"
        %li
          .row
            .col-sm-4
              total
            .col-sm-2
              = "$#{@order.total_price}"
