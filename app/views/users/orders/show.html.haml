.container
  .row
    .col-sm-12
      %h3
        = "Order ##{@order.id}"

      %ul
        - @order.line_items.each do |line_item|
          = render line_item

        %li
          .row
            .col-sm-4
              total
            .col-sm-2
              = "$#{@order.total_price}"
