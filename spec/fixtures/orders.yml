cards_order:
  state: <%= Order.states[:active] %>
  user: michelle

cards_order_completed:
  state: <%= Order.states[:paid] %>
  user: michelle

animal_cards_order:
  state: <%= Order.states[:active] %>
  user: paul

paid_animal_cards_order:
  state: <%= Order.states[:paid] %>
  user: paul
